{
  "name": "gtmux-relay-worker",
  "version": "0.1.0",
  "private": true,
  "description": "gtmux push relay: signs APNs JWT + forwards alerts to APNs",
  "scripts": {"deploy": "wrangler deploy", "typecheck": "tsc --noEmit"},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
