# Tasks — wrapper-launched-agents

- [ ] 1.1 Record the launch command on the resume record when the hook can see it
- [ ] 1.2 Resume with the recorded command; fall back to the registry form when absent
- [ ] 1.3 Keep the resumable/not-resumable decision untouched — only what gets typed changes
- [ ] 1.4 Tests: a wrapper-launched session resumes through its wrapper; a record without a command resumes as today; the evidence rules still refuse a pane that ran no agent
- [ ] 1.5 Verify command-level on an isolated server, with a wrapper script standing in for the real one
- [ ] 1.6 TROUBLESHOOTING: why a session came back as a bare shell, and how to tell "the save says shell" from "gtmux lost it"
- [ ] 1.7 Sync specs + archive
