# Tasks — wrapper-launched-agents

- [x] 1.1 Record the launch command on the resume record when the hook can see it
- [x] 1.2 Resume with the recorded command; fall back to the registry form when absent
- [x] 1.3 Keep the resumable/not-resumable decision untouched — only what gets typed changes
- [x] 1.4 Tests: a wrapper-launched session resumes through its wrapper; a record without a command resumes as today; the evidence rules still refuse a pane that ran no agent
- [x] 1.5 Verify command-level on an isolated server, with a wrapper script standing in for the real one
- [x] 1.6 TROUBLESHOOTING: why a session came back as a bare shell, and how to tell "the save says shell" from "gtmux lost it"
- [x] 1.7 Sync specs + archive
