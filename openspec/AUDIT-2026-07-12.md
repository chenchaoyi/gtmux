# Spec ⇄ Code audit — 2026-07-12

A full sweep of every `openspec/specs/<capability>` against today's code + tests
(15 parallel audit agents), triggered by the new "spec ⇄ code ⇄ test consistency"
rule in `CLAUDE.md`. `openspec validate --specs --strict` passes for ALL of these —
it only proves specs are well-formed, NOT that they match code; this audit is the
review-gate half the rule calls out.

**Result: 6 current · 9 partial · 0 fully-stale.** Reconciliation is tracked below;
check items off as their fix lands.

## Verdicts

| Capability | Verdict | Drift (one line) |
|---|---|---|
| notifications | ✅ current | — |
| chat-transcript | ✅ current | (one scenario lacks a dedicated test) |
| terminal-jump | ✅ current | — |
| mobile-chat-view | ✅ current | — |
| session-restore | ✅ current | (restore.log is diagnostics — no spec needed) |
| push-notifications | ✅ current | — |
| agent-radar | ⚠️ partial | "empty array when no tmux server" broken by native rows |
| mobile-pane-renderer | ⚠️ partial | scrollback cap: spec says 500, code is `MAX_LINES = 350` |
| native-agent-sessions | ⚠️ partial | #355 PID-liveness prune + `bg-spare` skip not in spec |
| remote-access | ⚠️ partial | paid "Direct" tier (`--redeem`) absent from spec |
| mobile-app | ⚠️ partial | spec requires removed "focus on Mac" (#85); dead code + stale comment |
| env-doctor | ⚠️ partial | Codex-notify scenario removed by #317; app-install now installs; cloudflared unlisted |
| browser-mirror | ⚠️ partial | whole "workbench" desktop mode + native/Elsewhere + bg modifier unlisted |
| terminal-theme | ⚠️ partial | mobile font map / font-size panel / pinch-zoom are dead controls (spec over-claims) |
| menu-bar-app | ⚠️ partial | glyph is color-only now (#160); self-update / right-click quit / bg modifier unlisted |

## Fix plan (by category)

### Category 2 — spec describes REMOVED behavior + dead code (lowest risk; do first)
- [x] **mobile-app** — spec still requires a Detail "focus on Mac" action calling `/api/focus`; removed in #85. Fix the spec scenario; clean the stale `DetailScreen.tsx` header comment ("Focus on Mac lives in the top bar"). (`client.focus()` stays — it's an intentional API-contract keeper.)
- [x] **env-doctor** — "Codex notify is single-slot" scenario describes behavior deleted by #317 (now wires the additive hooks system, never touches `notify`). Also "prints guidance for … install the app" is half-stale (#248 makes it actually install after consent; only tmux stays guidance-only).
- [x] **menu-bar-app** — "Ambient status item" scenario says the glyph encodes state by color + **shape** (red square + pause); since #160 it's ONE tinted brand-grid mark, color-only.

### Category 1 — code outgrew spec (sync-specs: add the shipped behavior)
- [x] **agent-radar** — "Structured output" scenario's "empty array when no tmux server" no longer holds once a native session is live (`gatherAgents` always appends `nativePanes`).
- [x] **mobile-pane-renderer** — update the "500" scrollback constant to 350.
- [x] **native-agent-sessions** — add the PID-liveness prune (process gone → immediate removal, comm-guarded pid-reuse) + the `bg-spare` sense-skip (#355).
- [x] **remote-access** — add the paid "Direct" tier (`gtmux tunnel --redeem <code>`, tunnel-worker `/direct/redeem`, `DIRECT_CODES` KV).
- [x] **env-doctor** — add the "Remote access" cloudflared check + `stepCloudflared` fixer (#61).
- [x] **browser-mirror** — add the desktop "workbench" mode (session rail, draggable/resizable pane board, layout presets, ⌘K palette; #218/#219/#223/#240), the native/Elsewhere category (#325), and the background-running modifier (#333).
- [x] **menu-bar-app** — add self-update / check-for-updates (#225 + the #351 `.updateFailed` watchdog), the right-click Quit menu (#340), and the background-running idle modifier.
- [x] **mobile-app** — fold the foreground-refresh trigger into the "Live radar via SSE" scenario.

### Category 3 — spec over-claims UNIMPLEMENTED behavior (needs a PRODUCT decision — NOT auto-fixed)
- [ ] **terminal-theme** — the mobile side never implements: `fontFamily` → bundled font (`nativeFontFamily()` always returns `'Menlo'`; the Settings font picker is a dead control), offline bundled fonts (woff2 are browser-mirror-only; native lost font-bundling when #346 removed xterm), and the "8–20pt + pinch-to-zoom" appearance panel (only DetailScreen A−/A+ with 3 presets 11–15). **DECIDE:** implement the mobile font/zoom features, OR downgrade the spec to "colors+palette on mobile; fonts/zoom are browser-mirror only." Until decided, the spec stays as-is with this known gap.

## Meta-findings (process, not a single capability)
- **#355 (this session) shipped native prune + bg-spare straight to code with no spec delta** — exactly the drift the new rule targets (it merged hours before the rule). → fixed by the native-agent-sessions item above.
- **The self-hosted-tunnel archive (2026-07-12) is imprecise**: its `tasks.md` marks "paywall gating" + "embed the Chisel client in-process" as P3-not-built, but both shipped later (the Direct paid tier + in-process chisel #341). The archived record understates reality. → the remote-access spec item captures the live behavior; the archive is a historical snapshot and stays.
- **The `2026-07-09-background-running-idle-modifier` change listed only `agent-radar` as modified** though its Impact names `macapp/.../AgentStore.swift` — so `menu-bar-app`'s spec never got the bg-modifier requirement. Contrast the parallel `errored-idle-sessions` change, which correctly listed `menu-bar-app`. → fixed by the menu-bar-app item above.
