/gtmux
*.test
/dist/
.DS_Store
gtmux-menubar
coverage.out
# Swift menu-bar app build artifacts
/macapp/.build/
/macapp/build/
/relay-bin
.playwright-mcp/

# Local release secrets (soft tunnel/relay gates baked at make app-release) — never commit
macapp/.release.env
