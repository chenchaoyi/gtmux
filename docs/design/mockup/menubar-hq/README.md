# Menu-bar HQ card · the report (menubar-hq-report, 2026-09-14)

Six artboards from the design canvas the commander chose from. `Main` (direction A,
red tier), `AOpenWaiting`, `ANormal` and `AReader` are what shipped; `DirectionB` and
`DirectionC` are the two alternatives, kept with their reasons in `canvas.json`'s notes.
Same format as `../gtmux-menubar.dc.html`; values lifted from `Theme.swift` / `MenuView.swift`.
