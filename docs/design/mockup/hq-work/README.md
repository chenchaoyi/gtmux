# HQ page · the "HQ's work" zone, analysed (hq-work, 2026-09-15)

Four artboards from the design canvas answering 「HQ's work 这一部分…用途价值不明，你做一些整体
分析」. The analysis is in `canvas.json`'s notes: the zone is the audit stream gtmux keeps of
what the supervisor did, already said in three other places, with nothing to act on, and
worth a full tab only if auditing were a daily act. `Main` (direction A, recommended) folds
the acts into the Console as timestamped rows beside HQ's own words and keeps the weekly
tally in the header's "HQ did" row (`HeaderDid`); `DirectionB` makes it a fourth door;
`DirectionC` keeps the tab as a filterable ledger. Same format as `../gtmux-mobile.dc.html`;
values lifted from `mobileapp/src/ui/theme.ts`, `HQHeader.tsx`, `ChatView.tsx`. Re-seed with
the `/design` helper to view; the seeded page is not committed.
