# 夜间自测记录 · 2026-09-09 夜 → 2026-09-10 晨

一个 worker 在 `gtmux-wt/test-nightly-selftest` worktree 里跑了一整夜。这份是给司令早上读的。

---

## 最前面:有一件事今天早上要做

**合进 main 不等于这台机器上的洞关上了。**

今晚发现并修掉了一个 guest 分享链接的越权问题。修复已经合进 main,但**这台机器跑的是已发布的版本,不是 main**。在升级之前,现有的两条 guest token 仍然能用。

解除风险只有两条路:

1. **撤销那两条 token** —— `gtmux pair revoke`,或直接删掉 `~/.config/gtmux/devices.json` 里 `scope: "guest"` 的两条;
2. 或者装上带修复的版本。

查到的事实(读的是 `~/.config/gtmux/devices.json`,只读没动):

| 名字 | 授权可见 pane | 最后活跃 | 来源 IP |
|---|---|---|---|
| `ccy` | 10 个(`%11 %14 %16 %20 %21 %26 %27 %28 %37 %52`) | 2026-08-20 | `154.26.189.69`(外网) |
| `niushaofeng` | 1 个(`%20`) | 2026-07-27 | 无记录 |

**两条都没有过期字段。** guest 这个 scope 是 2026-07-31 引入的(`4fc7c5e`),暴露窗口约 41 天。

这两条 token 能做什么,是实测出来的,不是读代码推的:

- 换出一个更高权限的凭据(device scope),够到 HQ 的板和设备名册 —— **已合入 main**(#1002)
- 读到白名单外**任意 pane** 的工作区 diff、agent 的**完整对话**、以及它此刻正在问的问题选项 —— #1004
- 把 owner 的终端拽到任意 pane —— **已合入 main**(#1003)

今晚十三个 PR 里,只有这一组有「现在就在外面」的性质。其余都是潜伏的或者只影响开发流程。

---

## 一句话结论

今晚十三条缺陷里,绝大多数是同一个病:**一个只会说「对」的检查**。守卫存在、测试存在、门禁存在,但它们守的不是自己声称守的东西。全量测试从头到尾是绿的,这些缺陷一个都没让它变红过。

更准的说法是:**守卫的覆盖范围是凭印象定的,而不是从被守的东西推导出来的。** 它有三种长相 —— 手写「查哪些东西」的清单、手写「打开哪些文件」的清单、凭对失败的记忆定下的阈值。

---

## 这个仓库怎么测(实测跑法)

| 层 | 跑法 | 规模 | 在 CI 里吗 |
|---|---|---|---|
| Go 核心 | `make check` = gofmt + vet + staticcheck(锁 v0.7.0)+ `go test -race ./...` | 241 个测试文件 / 38 个包 / 约 40 秒 | 是 |
| 设计与架构一致性 | `./scripts/check-design.sh` | 状态色、import 无环、图标尺寸、spec 校验、命令文档漂移、唤醒词表、pane 写入者声明、`$HOME` 收口、双语文档对、移动端发版说明 | 是 |
| 菜单栏 app | `cd macapp && swift build -c release && swift test` | 171 个 Swift 测试 | 是 |
| 手机 app(JS 层) | `cd mobileapp && npm run check` = tsc + eslint + jest | 80 个 suite / 877 个用例 | 是 |
| 推送中继 Worker | `cd relay-worker && npm run typecheck && npm test` | 7 个 golden payload 用例 | 是 |
| 隧道 Worker | `cd tunnel-worker && npm run typecheck && npm test` | 4 个用例 | 是 |
| **模拟器自动化** | `cd mobileapp && npm run e2e:build && npm run test:e2e` | 27 个 Appium suite + 5 个 fake-serve 契约测试 | **否** |
| **Go 集成测试** | `GTMUX_RESTORE_E2E=1` / `GTMUX_IT=1 go test ./internal/app/` | restore 契约 7 个子用例等 | **否** |

开工时先跑了一遍全部不吃磁盘的部分:**全绿**,零失败。所有缺陷都是在这个绿的基础上找出来的。

关于「模拟器自动化测试可能根本不存在」的猜测:**它存在,而且相当完整**。缺口不在「有没有」,在「跑不跑得到」。

---

## 十三个 PR

| PR | 内容 | 性质 |
|---|---|---|
| #992 | 守卫误伤了隔离做对了的测试(`/tmp` 短路径) | 开发流程 |
| #993 | 守卫自称「唯一收口」,实际 23 个旁路;8 个测试在读本机真实配置 | 开发流程 |
| #994 | 防 QR 踩坑的测试,查的方向是反的 | 会漏掉真 bug |
| #995 | restore 契约测试信任隔离但不证明它 | 有破坏面 |
| #996 | e2e 跳过三分之二还报绿 | 覆盖假象 |
| #997 | 章程改了不提版本号,没人拦 | 改动到不了用户 |
| #998 | 中文那半文档没有任何东西在查,也修不了 | 文档漂移 |
| #999 | 唤醒词表门禁只读英文那一半 | 文档漂移 |
| #1000 | doctor 把你正坐在里面的终端列进「其它终端」 | **用户今天可见** |
| #1001 | 固化:CONTRIBUTING + 双语文档对门禁 | 制度 |
| #1002 | guest 链接可换成 device token | **安全,提权** |
| #1003 | guest 可把 owner 屏幕拽到任意 pane | **安全,骚扰面** |
| #1004 | guest 可读任意 pane 的 diff / 对话 / 选项 | **安全,内容泄露** |

### 几条值得单独看的

**#993 —「唯一收口」是一句从没人验证过的断言。** `state.go` 的注释把这个包描述成「every gtmux path 都要经过的唯一收口」,并以此解释守卫为什么放在这里而不是每个写入口。实际有 23 个文件自己读 `$HOME`,包括装 hook 时改 `~/.claude/settings.json`、serve 的推送 token 和设备名册、配对凭据。全部收口后抓到 8 个测试在读本机真实配置,最典型的一个:`driver/registry_wiring_test.go` 断言「每个装了 hook 的 agent 都有 Receipt 和 Ready」,而它调用的 `driver.For()` 会读真实 config 里的 kill-switch —— 那正是唯一能让这条断言变假的东西。它在谁的机器上都绿,但绿的原因跟被测逻辑无关。

**#994 — 名字对、注释对、意图对,断言的方向反了。** `qr.go` 顶上全大写警告别用 quadrant blocks(PR #179 因此被回滚),以它命名的测试查的是「宽度别超过 60 列」,而 quadrant 渲染会把列数**减半**。把 quadrant 渲染器原样放回去,三个 QR 测试全绿。

**#1002/#1003/#1004 — 同一个根因在授权面上。** `fullOnly` 的注释里已经记着它关掉过一模一样的洞,就在隔壁的设备列表路由上。那次修复打在了「被发现的地方」而不是「这条性质所在的地方」,于是相邻路由把洞留了 41 天。这次三条一起收在一个共用闸门 `mayReachPane` 上。

---

## 每条修复的注入验证

要求是「把守卫拿掉,测试必须变红」。逐条记录:

| PR | 注入点 | 结果 |
|---|---|---|
| #992 | 去掉 `/tmp` 白名单 | 「短 /tmp 目录算可丢弃」变红 |
| #992 | 把可丢弃根放宽到 `/` | 「操作者真实 HOME 不可丢弃」变红 |
| #993 | 把 `usercfg.Path()` 改回直接读 `$HOME` | 门禁点名该文件,构建失败 |
| #994 | 放回 quadrant 渲染器 | 报「19 glyphs across a 38-module row」变红 |
| #995 | 在隔离 socket 上先起一个占位 session | 报「not isolated … holding [squatter]」变红 |
| #996 | 严格模式下跑一个会被跳过的 suite | 退出码 1 |
| #996 | 严格模式下跑一个**会执行**的 suite | 退出码 0(反向验,否则就是无差别报错) |
| #997 | 改英文章程不动版本号 | 红,并打印该填的新指纹 |
| #997 | 只改中文章程 | 红 |
| #997 | 把指纹改成只哈希英文一半 | 第二个测试红 |
| #998 | 中文文档 rendered 区块写错 | 红并点名 `cli.zh.md`;`docs-fix` 现在能修回去 |
| #998 | 新增一份带标记区块的文档 | 自动被覆盖并变红,不需改代码 |
| #999 | 四处(中英文档 × 中英章程)各去掉一个唤醒类别 | 四处各自点名变红 |
| #1000 | 退回旧的跳过条件 | 三个子用例(kitty/wezterm/appleterminal)全红 |
| #1000 | 把「是否支持」换回硬编码副本 | **只问今天的那条测试通过(抓不到)**,摆出新注册表的那条变红 |
| #1001 | 新增无孪生的用户文档 / 只有中文 / 例外指向不存在的文件 | 三个方向各自变红 |
| #1002 | 拿掉 `fullOnly` 闸门 | 链路测试报「the link was traded up」变红 |
| #1003 | 拿掉 focus 的闸门 | 报「owner 的终端被一个看不到它的 guest 移到了 %2」 |
| #1004 | 三条闸门全部拿掉 | 三条各自变红,各自打印泄露出的内容 |

---

## 今晚最耐久的一条

**「写下来给别人看」和「心里觉得没问题」之间,隔着的正是这整个母题。**

枚举全部认证路由查 scope 判定时,14 条没有判定,我把 `handleDiff` 和 `handleTranscript` 判成了无害 —— 那是**推的**,探针返回 503 只是因为测试夹具没接那两个依赖。

后来准备把这份判断**写成一张给下一个人看的表**,于是决定一条都不推、全部实测。两条现形了:guest 能读到白名单外任意 pane 的 diff 和完整对话。第三条 `options` 也是这么翻出来的。

前面几层是**守卫的覆盖范围**凭印象定;这一层是**我自己的结论**凭印象定。唯一逼出差别的动作,是「这要写给别人看」。

同一个形状还长在操作习惯上:今晚有三次,我用 `git checkout -- <file>` 还原注入时,把同一个文件里**尚未提交的修复**一起还原了。还原的范围也是凭印象定的,不是从「我注入了什么」推导的。第三次之后改成先提交再注入。

---

## 查了、确认不用改的(结论同样有效)

- **doctor 的 28 个检查项全部接线了**,没有孤儿。而且 staticcheck 本来就会抓没接线的未使用函数,再加一道守卫是重复劳动。
- **`allowedSendKeys` 白名单做得是对的**:除了正反两份名单,还有一条计数守卫(`len(allowedSendKeys) != len(allowed)`),任何增删都会红,集合是钉死的。
- **死代码基本干净**。staticcheck 一直开着;另外扫了一遍「包外零引用的导出标识符」,剩下的全是接口方法和值类型方法,扫描器分不清而已。

---

## 还没查的

**活 serve 那 16 个 e2e suite 没跑。** 判断是不冒险:仓库里有两条直接先例 —— 「隔离 HOME 挡不住启动 app、对活 tmux 下命令这类全局副作用」(当时拿 mutating 命令当探针把菜单栏跑成了僵尸),以及「两个 serve 同时跑会让配对报过期」。`gtmux serve` 是常驻的、会驱动 tmux、会往 pane 里打字的进程,深夜无人看着时起一个正落在这两条警告范围内。**要跑这 16 个,需要决定起一个隔离 serve 实例的方式。**

**e2e 和两个 Go 集成测试都不在 CI 里。** 塞进 CI 是有成本的决定(时长、机器、macOS runner)。两个 Go 集成测试今晚手动跑过,都是绿的。

**`internal/app` 15822 行非测试代码**,测试 7885 行,比值 0.50,是大包里最低的(`hq` 0.79,`dispatch` 1.26,`server` 0.88)。翻了 doctor 和 serve 两条线;`doctor.go` 1741 行、28 个检查项里 **20 个没有任何测试碰过**,没动,那是另一个量级的活。

**`/api/upload` 没有任何 scope 判定。** guest 可以往 owner 磁盘写文件(单次上限 30MB,次数无限)。不修的理由是该用哪道闸不明显:按 pane 授权它没有 pane 参数,按 `fullOnly` 会挡掉有输入权限的 guest 发照片。这是产品决定,列出来不拍板。

**一条低价值观察,列出来不修:** `internal/hq/slowtick_test.go` 的 `TestResourceTierKey` 用 `resource.MachineTier(m).String()` 做断言的标准答案,而被测函数的实现就是这一句。它测不出 `MachineTier` 本身算错。

---

## 环境与清理

- 全程在 `gtmux-wt/test-nightly-selftest` worktree 里做,没碰主仓工作树。
- `node_modules` 用软链复用主仓,省 660MB;移动端构建复用主仓已有的 Pods 和热缓存,省约 3GB。
- 自建模拟器 `gtmux-selftest`(UDID `BA72804E-E4C0-4549-AEE7-01ABB465072C`),**没有碰**司令那台启动着的 iPhone 17 Pro,也没动关机那台 17 Pro Max。
- Swift 构建产物(243MB)跑完即删。收工时会删掉自建模拟器和 `.e2e-artifacts`。
