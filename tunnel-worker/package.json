{
  "name": "gtmux-tunnel-worker",
  "version": "0.1.0",
  "private": true,
  "description": "gtmux tunnel control plane: provisions per-Mac Cloudflare named tunnels under *.gtmux.ccy.dev",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
